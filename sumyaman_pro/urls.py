from django.contrib import admin
from django.urls import path

urlpatterns = [
    path('admin/', admin.site.urls),
    
    # Dashboard & Wings
    
    # Detail Pages
    
    # AJAX APIs (Working... Fix)
]
